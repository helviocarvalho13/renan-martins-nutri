import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Disponibilidade | Painel Administrativo - Renan Martins",
};

export default function DisponibilidadeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
