import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pacientes | Painel Administrativo - Renan Martins",
};

export default function PacientesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
